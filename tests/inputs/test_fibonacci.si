def fib(a)
  if (a < 3) then 1 else
    fib(a - 1) + fib (a - 2)
  end
end
def main()
  x = 1;
  while (x <= 10) do
    output = concat(to_s(fib(x)), " ");
    print_string(output);
    x = x + 1
  end;
  "end"
end
